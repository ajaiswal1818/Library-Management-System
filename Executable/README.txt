Requirements:
1) Adobe Reader

To run the executable, Click on Assignment Shortcut in Executable Folder. To run the executable,
copy the Entire Executable folder to preserve the directory structure inside the executable folder.